kind: ConfigMap
apiVersion: v1
metadata:
  labels:
    app: homeroom
    role: lab
  name: homeroom-cfg-{{_lab_name}}
data:
  workshops.json: | -
    [
      {
        "title": "{{_lab_name}}",
        "description": "{{lab_description}}",
        "url": "{{__lab_url}}"
      }
    ]