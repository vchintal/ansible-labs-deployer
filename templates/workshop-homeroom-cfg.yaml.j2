kind: ConfigMap
apiVersion: v1
metadata:
  labels:
    app: homeroom
    role: lab
  name: homeroom-cfg-{{ lab_workshop_app_name }}
data:
  workshops.json: | -
    [
      {
        "title": "{{ lab_workshop_description }}",
        "description": "{{ lab_workshop_description }}",
        "url": "{{ __lab_url }}"
      }
    ]